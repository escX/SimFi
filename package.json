{
  "name": "simfi",
  "version": "0.2.0",
  "description": "Contracts for simulated finance",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat ignition deploy --parameters ignition/parameters.json --network localhost",
    "visualize": "npx hardhat ignition visualize",
    "start": "npx hardhat compile && npx hardhat write && npx hardhat node"
  },
  "author": "escX",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.6",
    "@nomicfoundation/hardhat-ethers": "^3.0.5",
    "@nomicfoundation/hardhat-ignition-ethers": "^0.14.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.1",
    "hardhat": "^2.21.0",
    "hardhat-gas-reporter": "^1.0.10"
  },
  "dependencies": {
    "@chainlink/hardhat-chainlink": "^0.0.4",
    "@openzeppelin/contracts": "^5.0.2",
    "secp256k1": "^5.0.0"
  }
}
